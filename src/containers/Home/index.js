import React from "react";
import PropTypes from "prop-types";
import JobsMatcher from "../../components/JobsMatcher";

const Home = () => {
  return (
    <>
      <JobsMatcher />
    </>
  );
};

Home.propTypes = {};

export default Home;
